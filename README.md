## Important Notes

I made the following assumptions:

1. **Capture size** should be a multiple of 10 (10, 20, 30, etc.), which means sending an integer number of frames (NOT subframes).
2. **ORAN Data type** is always fixed and read from the IQ file.
3. The program supports up to **SC = 60**.
